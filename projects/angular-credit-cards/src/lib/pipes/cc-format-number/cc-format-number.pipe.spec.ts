import { CcFormatNumberPipe } from './cc-format-number.pipe';

describe('CcFormatNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new CcFormatNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
